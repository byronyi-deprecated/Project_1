#ifndef RECTDIALOG_H
#define RECTDIALOG_H

#include <QDialog>

class RectDialog : public QDialog
{
    Q_OBJECT
public:
    explicit RectDialog(QWidget *parent = 0);
    
signals:
    
public slots:
    
};

#endif // RECTDIALOG_H
